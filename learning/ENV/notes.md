# ENV notes

- **uv version:**
- **Python version `hello.py` printed:**
- **What the `# /// script` block is for:**

## Stretch goal (optional)

Only if you attempted the ENV stretch. Remember to put `hello.py` dependencies back to `[]`.
