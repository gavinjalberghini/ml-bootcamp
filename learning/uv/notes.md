# uv notes

- **uv version:**
- **Python version `hello.py` printed:**
- **What the `# /// script` block is for:**

## Stretch goal (optional)

Only if you attempted the uv stretch. Remember to put `hello.py` dependencies back to `[]`.
