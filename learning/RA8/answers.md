# RA8 answers

## What is the difference between training, validation, and test sets? Where does leave-one-out sit?

## Why is “pick the best LOO `k` and report that LOO score” optimistic?

## How does nested validation differ from a single holdout?

## How should accuracy and macro-F1 move as `k` grows on `small.arff`?

## What do vote fractions tell you that a hard label does not?

## Stretch goal (optional)

