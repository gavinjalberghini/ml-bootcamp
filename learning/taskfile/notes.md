# taskfile notes

- **Task version:**
- **What a Taskfile is for (vs running `uv run` yourself):**
- **How you would pass a different data file into `task knn`:**
- **Why this Taskfile lives under `learning/taskfile/`:**
