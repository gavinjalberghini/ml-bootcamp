# RA6 answers

## What is class imbalance, and why can high accuracy still mean the model is failing? Use `small.arff` or your PA2 report.

## How do oversampling, undersampling, and cost-sensitive learning differ?

## Why do imbalance methods that assume a fixed class ratio go stale on a stream?

## How can concept drift and class imbalance interact? Look at `small_stream.arff` weights.

## How might imbalance affect kNN votes, and what window or sampling changes could help?

## Stretch goal (optional)

